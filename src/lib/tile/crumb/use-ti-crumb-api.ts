import { CrumbProps, CrumbEmitter } from "./ti-crumb-types";

export type TiCrumbApi = ReturnType<typeof useTiCrumbApi>;

export function useTiCrumbApi(_props: CrumbProps, _emit: CrumbEmitter) {
  //-----------------------------------------------------
  // 返回接口
  //-----------------------------------------------------
  return {};
}