import { ComPropExample } from '../../../../core';
import { EditCodeProps } from '../ti-edit-code-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as EditCodeProps,
} as ComPropExample;
