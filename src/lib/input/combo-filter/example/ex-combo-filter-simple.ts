import { ComPropExample } from '../../../../core';
import { ComboFilterProps } from '../ti-combo-filter-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as ComboFilterProps,
} as ComPropExample;
