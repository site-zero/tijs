import { ComPropExample } from '../../../../core';
import { ScoreProps } from '../ti-score-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as ScoreProps,
} as ComPropExample;
