import { ComPropExample } from '../../../../_type';
import { ScoreProps } from '../ti-score-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as ScoreProps,
} as ComPropExample;
