import { ComPropExample } from '../../../../_type';
import { ScoreProps } from '../ti-score-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {
    value: 6.8,
  } as ScoreProps,
} as ComPropExample;
