import { ComPropExample } from '../../../../core';
import { SorterProps } from '../ti-sorter-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as SorterProps,
} as ComPropExample;
