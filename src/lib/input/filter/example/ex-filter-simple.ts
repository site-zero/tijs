import { ComPropExample } from '../../../../core';
import { FilterProps } from '../ti-filter-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as FilterProps,
} as ComPropExample;
