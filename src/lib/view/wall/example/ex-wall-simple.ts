import { ComPropExample } from '../../../../_type';
import { WallProps } from '../ti-wall-types';

export default {
  name: 'simple',
  text: 'i18n:simple',
  comConf: {} as WallProps,
} as ComPropExample;
