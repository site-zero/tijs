import {
  TreeEmitter,
  TreeProps
} from './ti-tree-types';

export function useTreet(_props: TreeProps, _emit: TreeEmitter) {

  //-----------------------------------------------------
  // 输出特性
  //-----------------------------------------------------
  return {

  };
}
