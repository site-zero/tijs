import { getLogger } from '../../../core/log/ti-log';
import {
  TreeEmitter,
  TreeProps
} from './ti-tree-types';

const log = getLogger('TiList.use-list');

export function useList(_props: TreeProps, _emit: TreeEmitter) {

  //-----------------------------------------------------
  // 输出特性
  //-----------------------------------------------------
  return {

  };
}
