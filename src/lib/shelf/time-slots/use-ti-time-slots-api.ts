import { TiTimeSlotsProps, TiTimeSlotsEmitter } from "./ti-time-slots-types";

export type TiTimeSlotsApi = ReturnType<typeof useTiTimeSlotsApi>;

export function useTiTimeSlotsApi(_props: TiTimeSlotsProps, _emit: TiTimeSlotsEmitter) {
  //-----------------------------------------------------
  // 返回接口
  //-----------------------------------------------------
  return {};
}