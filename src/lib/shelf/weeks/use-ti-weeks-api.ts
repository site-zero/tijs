import { TiWeeksProps, TiWeeksEmitter } from "./ti-weeks-types";

export type TiWeeksApi = ReturnType<typeof useTiWeeksApi>;

export function useTiWeeksApi(_props: TiWeeksProps, _emit: TiWeeksEmitter) {
  //-----------------------------------------------------
  // 返回接口
  //-----------------------------------------------------
  return {};
}